# 📝 DevSecOps 파이프라인 구축 인수인계 및 현황 백업 노트

본 문서는 프로젝트 재부팅 또는 세션 초기화 시, AI 코딩 어시스턴트가 이전의 개발 히스토리, 해결된 이슈, 현재 진행 단계 및 다음 액션 플랜을 100% 그대로 복원하여 이어서 작업할 수 있도록 기록된 공식 인수인계서입니다.

---

## 1. 🎯 프로젝트의 원래 목표 및 계획
* **최종 목표**: 깃허브 코드가 커밋/푸시되었을 때, 젠킨스(Jenkins) CI/CD 파이프라인에서 자동으로 소스코드 및 도커 이미지 취약점을 스캔하고(Syft & Grype), 취약점이 발견되면 빌드를 안전하게 중단(Fail-Fast)시키며, 최종 통과된 SBOM(소프트웨어 자산 명세서)을 Dependency-Track 서버로 전송하고 웹 서비스를 배포하는 철저한 DevSecOps 환경 구축.
* **VEX의 결합**: 도커 베이스 OS 및 톰캣 엔진 등 실행상 무해하지만 기계적으로 검출되는 환경적 취약점(오경보)들을 우회시키기 위해 국제 표준 VEX 규격을 모방한 예외 처리 파일(`.grype.yaml`)을 적용하여 보안 차단 옵션을 켠 채로 빌드를 최종 성공시키는 검증 시나리오 수립.

---

## 2. 🛠️ 그동안 발생한 문제 및 해결 이력 (Troubleshooting)

### ① 젠킨스 내부 보안 도구 부재 문제
* **현상**: 2단계 스캔 시 `syft: not found` 에러와 함께 빌드가 마비됨.
* **원인**: 젠킨스가 도커 컨테이너 내부에서 동작하는데 컨테이너 안에 스캔 바이너리가 없었음.
* **해결**: 젠킨스 컨테이너 내부(`/usr/local/bin/`)에 `syft`와 `grype` 최신 바이너리를 직접 설치하여 해결.

### ② Dependency-Track 포트 포워딩 및 통신 장애
* **현상**: 젠킨스가 디트랙으로 SBOM 전송 시 `Connection Refused` 혹은 API 500 에러 발생.
* **원인**: 디트랙 API 서버의 실제 내부 포트가 `8080`이었으나 컴포즈 맵핑 오류가 있었고, 젠킨스 파일 내 `DTRACK_URL`이 컨테이너 내부의 `localhost`를 바라보고 있어 통신 단절됨.
* **해결**: 디트랙 포트 맵핑을 `8089:8080`으로 전격 교정하고, 젠킨스 파일의 `DTRACK_URL` 주소를 호스트 VM IP인 `http://192.168.111.130:8089`로 변경하여 완벽하게 뚫어냄.

### ③ 이전 빌드 찌꺼기(build/)로 인한 스캔 오염
* **현상**: `build.gradle`에서 취약점을 이미 제거했음에도 이전 빌드 잔재 때문에 2단계 스캔이 계속 실패함.
* **원인**: `syft` 재귀 스캔이 과거 빌드 결과물인 `backend/build/libs/*.jar` 안의 찌꺼기 라이브러리까지 긁어모아 취약점으로 인식함.
* **해결**: `Jenkinsfile` 2단계 시작 시점에 `sh 'rm -rf backend/build'` 코드를 삽입하여 스캔 전 디렉토리 정밀 청소 가동.

### ④ 프론트엔드 버전 잠금(package-lock.json) 고착 문제
* **현상**: `package.json`에서 axios 등을 업데이트했으나 `form-data 4.0.5` 취약점이 계속 검출됨.
* **원인**: 기존에 깃에 존재하던 `package-lock.json` 파일이 옛날 버전을 강하게 홀딩하고 있었음.
* **해결**: 로컬 `frontend/package-lock.json`을 강제 삭제한 후 `npm install`을 다시 돌려 락 파일을 최신 보안 스펙(4.0.6 이상)으로 재조립 후 푸시하여 해결.

---

## 3. 🏁 현재 아키텍처 및 진행 상태 (Milestone)
* **2단계 (소스코드 스캔)**: `--fail-on high` (차단 옵션)이 **장착된 채로 완벽하게 무사 통과 (No vulnerabilities found)** 성공. (백/프론트 소스코드 무결점 달성)
* **5단계 (Docker 이미지 스캔)**: `--fail-on high` (차단 옵션)이 **장착된 상태로 다시 복구 완료**. (베이스 이미지인 우분투 20.04 OS와 내장 톰캣의 High/Critical 취약점으로 인해 현재 빌드가 의도적으로 중단(FAILURE)된 상태)
* **아카이브 다운로드 결합**: 빌드 성공 시 생성된 모든 `*-sbom.json` 파일이 젠킨스 웹 UI 페이지 우측 상단 `Artifacts` 영역에 박제되어 **크롬 브라우저에서 개인 PC로 편리하게 다운로드받을 수 있는 기능 장착 완료**.

---

## 4. 🚀 재부팅 후 이어서 진행할 다음 단계 (Next Action Plan)

사용자님이 가상머신과 PC를 재부팅하고 돌아오시면, 아래 순서대로 최종 VEX 실습을 진행하여 이 프로젝트를 영광의 피날레로 이끄시면 됩니다:

1. **5단계 실패 로그에서 CVE 수집**:
   * 재부팅 후 젠킨스 웹에 접속하여, 방금 실패한 빌드(차단 옵션이 켜져서 실패한 빌드)의 5단계 로그에서 `CVE-XXXX-XXXX` 번호들을 수집합니다.
2. **로컬 VEX 파일 `.grype.yaml` 작성**:
   * 수집한 CVE 번호들을 예외 처리해 주도록 프로젝트 루트 폴더에 `.grype.yaml` 파일을 수동 생성하여 아래 구조로 작성합니다:
     ```yaml
     ignore:
       - vulnerability: CVE-XXXX-XXXX
         reason: "Mitigated by reverse proxy / Not used in codebase"
       - vulnerability: CVE-YYYY-YYYY
         reason: "Base OS package vulnerability. Accepted risk."
     ```
3. **깃 푸시 및 최종 성공 검증**:
   * VEX 파일(`.grype.yaml`)을 Git에 추가하고 푸시합니다.
   * `Jenkinsfile`에 차단기 옵션(`--fail-on high`)이 여전히 켜져 있음에도 불구하고, 이 VEX 필터 덕분에 5단계까지 무사히 녹색 불로 통과하여 **최종 배포 성공 및 Dependency-Track 자동 업로드 연동이 성공하는 드라마틱한 장면**을 확인합니다!
