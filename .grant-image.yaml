# Grant License Compliance Configuration for Container Images
format: table
quiet: false
verbose: false

require-license: false
require-known-license: false

# Skip license auditing for all packages in the container image, 
# as they are already strictly verified during Stage 2 source-level checks.
ignore-packages:
  - "*"

allow:
  - "*"
